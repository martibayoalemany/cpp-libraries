#include "BoostDi.h"

/*
#include <boost/di.hpp>

namespace di = boost::di;

void BoostDi::execute() {
    const auto injector = di::make_injector(
            di::bind<int>.to(42),
    di::bind<double>.to(87.0)
    );

    injector.create<example>();
}
}
 */
